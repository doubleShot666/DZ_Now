<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_daily_articles"
                android:icon="@drawable/ic_menu_camera"
                android:title="Articles du jour"/>
        <item
                android:id="@+id/nav_local_articles"
                android:icon="@drawable/ic_menu_gallery"
                android:title="Articles locaux"/>
        <item
                android:id="@+id/nav_saved_articles"
                android:icon="@drawable/ic_menu_slideshow"
                android:title="Articles sauvegardés"/>
        <item
                android:id="@+id/nav_themes"
                android:icon="@drawable/ic_menu_manage"
                android:title="Thèmes"/>
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                    android:id="@+id/nav_options"
                    android:icon="@drawable/ic_menu_manage"
                    android:title="Options"/>
            <item
                    android:id="@+id/nav_exit"
                    android:icon="@drawable/ic_menu_send"
                    android:title="Quitter"/>
        </menu>
    </item>

</menu>
